


assumptions/assertions:
- MainLoop is extremely fast




allocation:
- further increase reliability of the system by 
  using a custom allocator with a preallocated buffer of ~128mb
  instead of Global since max memory footprint is known at compile time.

shared state/usage of Box/Arc<RwLock>:
- Replace with better pattern like Qt/Qml's sync pattern or 

- Since server application

serial_interface:
- shared solution for devices controlled directly via usb or a serial interface like the wago 750-653 

server/src/main.rs:
- tight coupling with LaserMachine inside handle_serial_device_hotplug

machines/src/serial/registry.rs:
- new_serial_device is unused. What is it's purpose?
- each device is directly coupled to a machine (). Should be modular


docs/actors.md:
- trait Actors doesn't exist, either define or remove docs
